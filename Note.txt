install Npcap

pip install scapy

Scapy fait simplement une résolution automatique de nom de service : 
il existe une table de correspondance standard (héritée d'Unix, fichier /etc/services,
mais Python a son propre équivalent intégré via le module socket) qui associe des numéros de port à des noms de service connus :

Installation de nmap sous Windows

Scanner le port d'un site web avec nmap :

Utiliser Fing scanner reseau

Point important sur ce que Fing va réellement produire

Fing (comme la plupart des scanners mobiles) fait généralement un scan de type TCP Connect (poignée de main complète), pas un vrai half-open SYN scan comme nmap sur PC. Ça reste détectable par notre script, car :

Le premier paquet envoyé pour chaque port testé est toujours un SYN pur (flags == "S") — c'est la norme du protocole TCP, tout scan commence forcément par ça, peu importe l'outil.
Notre détecteur ne regarde que ce premier paquet SYN, donc ça devrait déclencher l'alerte normalement.

half-open SYN scan (nmap -sS 192.168.10.113)

1. Le SYN est envoyé
2. Si le port est ouvert : le serveur répond SYN-ACK
3. Le scanner envoie RST (Reset) au lieu de ACK
4. Le serveur marque le port comme ouvert

Le gros avantage pour l’attaquant : il n’a jamais complété la poignée de main, donc le serveur ne garde pas de trace complète de la connexion.

Pourquoi ton script détecte ça ?
Parce que pour effectuer ce scan, le scanner doit forcément commencer par envoyer un paquet SYN ( SYN scan → SYN → RST).
Notre détecteur se base uniquement sur l’envoi de ce premier paquet SYN avec flags == "S", que ce soit un scan complet ou half-open.

En résumé :

Scan TCP connect complet → on voit SYN → SYN-ACK → ACK
Scan half-open SYN → on voit SYN → SYN-ACK → RST
Dans les deux cas, ton détecteur voit bien le SYN de départ et déclenche l’alerte sans problème.

socket

Étape 2 terminée — Détection de scan de ports validée de bout en bout
Récapitulatif de ce que tu maîtrises maintenant :

Capture réseau bas niveau (raw sockets, mode promiscuous, Npcap)
Structure des couches Ethernet/IP/TCP
Les flags TCP et la poignée de main (SYN, SYN-ACK, ACK, FIN)
La technique du SYN scan (half-open) utilisée par nmap
La conception d'une règle de détection avec fenêtre glissante et seuil
Le debugging méthodique de trafic réseau réel



Phase 1 : Comprendre ce qui se passe

Ce que fait ton script
Il capture TOUT le trafic TCP passant sur ton interface
Quand il voit un paquet avec le flag SYN flag == "S"
Il stocke l’IP source + port destination + timestamp
Il nettoie les entrées trop vieilles (> 10 secondes)
Si le nombre de ports différents scannés depuis une IP dépasse 15 en 10s → ALERTE

Ce qui se passe réellement quand un scan de ports se produit

Un scanner (ex: nmap) envoie des paquets SYN vers différents ports de ta machine.
Ton script voit ces SYN et compte les ports distincts.

Exemple avec nmap -p 80,443,21,22,23,20,81,82,83,84,85,86,87,88,89,90 192.168.137.1

Il envoie : SYN → port 80
Il envoie : SYN → port 443
Il envoie : SYN → port 21
...
Il envoie : SYN → port 90

Ton script voit ces 16 SYN, compte 16 ports distincts, et déclenche l’alerte.

Le flux TCP complet d’une connexion (poignée de main)


a poignée de main TCP normale :
Client → SYN → Serveur
Client ← SYN-ACK ← Serveur
Client → ACK → Serveur   (connexion établie)

Quand le serveur reçoit un SYN, il ne se contente pas de répondre — il réserve de la mémoire
pour cette connexion en attente (une structure appelée TCB - Transmission Control Block, stockée dans une
ile d'attente appelée backlog). Il attend ensuite l'ACK final pour finaliser.



Étape 3 terminée — Détection de flood validée

Récapitulatif de ce qu'on a ajouté à ta maîtrise :

Compréhension du mécanisme d'attaque SYN flood (saturation de backlog)
Distinction entre signature "diversité" (scan) et signature "volume" (flood)
Le concept d'alert fatigue, un vrai problème de sécurité opérationnelle
Implémentation d'un mécanisme de cooldown/throttling d'alertes


Étape 5 — Détection d'usurpation ARP (ARP Spoofing)